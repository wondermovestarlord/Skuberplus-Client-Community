# 이슈 생성 시 Clawpod 웹훅 전송

name: Issue Webhook

on:
  issues:
    types: [opened]

jobs:
  notify:
    name: Send Webhook
    runs-on: ubuntu-latest
    steps:
      - name: Send webhook
        env:
          WEBHOOK_URL: ${{ secrets.CLAWPOD_WEBHOOK_URL }}
          WEBHOOK_SECRET: ${{ secrets.CLAWPOD_WEBHOOK_SECRET }}
        run: |
          PAYLOAD=$(jq -n -c \
            --arg event "issue_opened" \
            --arg repo "${{ github.repository }}" \
            --arg issue_number "${{ github.event.issue.number }}" \
            --arg title "${{ github.event.issue.title }}" \
            --arg author "${{ github.event.issue.user.login }}" \
            --arg url "${{ github.event.issue.html_url }}" \
            '{event: $event, repo: $repo, issue_number: $issue_number, title: $title, author: $author, url: $url}')
          SIGNATURE=$(echo -n "$PAYLOAD" | openssl dgst -sha256 -hmac "$WEBHOOK_SECRET" | awk '{print $2}')
          curl -f --retry 3 --max-time 30 \
            -X POST "$WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -H "X-Hub-Signature-256: sha256=$SIGNATURE" \
            -d "$PAYLOAD"
